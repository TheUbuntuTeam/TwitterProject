from setuptools import setup, find_packages

setup(
    name='extract_script_v_alpha',
    version='alpha',
    description='a script to extract tweet',
    author='UbuntuTeam',
    packages=find_packages(),
)
